# -TypeScript-Three.js--3D-
前端使用 Three.js + TypeScript 渲染 3D 场景（挡板、小球、砖块矩阵）。C++ 实现核心物理：球与挡板、球与砖块的碰撞响应，通过 WebAssembly 导出函数供前端每帧调用。Node.js 提供 REST API 获取预设关卡（砖块布局）和保存用户自建关卡（JSON）。实现鼠标/键盘控制挡板，小球碰撞砖块后得分，消除全部砖块过关。随机掉落道具（加长挡板、多球、慢速）。包含 5 个内置关卡，支持本地存储最高分。主题切换（光影风格）。输出前端、C++（含编译脚本）、Node.js 代码。
